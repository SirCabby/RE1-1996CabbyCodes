#pragma once

// RE1CC_VERSION is defined by the build from the VERSION file (see the Makefile).
// The fallback only applies to ad-hoc compiles that bypass it.
#ifndef RE1CC_VERSION
#define RE1CC_VERSION "0.0.0-dev"
#endif
